type Vocabulary {
  id: ID!
  word: String!
  meaning: String!
  example: String
  languageName: String!
  createdAt: DateTime!
  updatedAt: DateTime!

  storyId: ID
  userId: ID!
}

scalar DateTime

extend type Query {
  vocabularies: [Vocabulary!]!
  vocabulary(id: ID!): Vocabulary
}