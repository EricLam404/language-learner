type User {
  id: ID!
  email: String!
  username: String!
  createdAt: DateTime!
  updatedAt: DateTime!
  languages: [UserLanguage!]
  vocabularies: [Vocabulary!]
  stories: [Story!]
  worksheets: [Worksheet!]
}

scalar DateTime

extend type Query {
  users: [User!]
  user(id: ID!): User
}