type Progress {
  id: Int!
  userId: Int!
  user: User!
  languageName: String!
  language: Language!
  completedAt: DateTime!
  type: ProgressType!
}

enum ProgressType {
  VOCABULARY
  STORY
  WORKSHEET
}

scalar DateTime