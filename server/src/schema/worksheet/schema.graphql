type Worksheet {
  id: Int!
  content: String!
  userId: Int!
  languageName: String!
  createdAt: DateTime!
  updatedAt: DateTime!
  completedAt: DateTime
}

extend type Query {
  worksheets: [Worksheet!]!
  worksheet(id: Int!): Worksheet
}