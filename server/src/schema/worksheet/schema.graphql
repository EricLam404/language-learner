type Worksheet {
  id: Int!
  content: String!
  user: User!
  language: Language!
  completedAt: DateTime
  createdAt: DateTime!
  updatedAt: DateTime!
}

extend type Query {
  worksheets: [Worksheet!]!
  worksheet(id: ID!): Worksheet
}