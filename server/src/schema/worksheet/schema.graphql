type Worksheet {
  id: Int!
  content: String!
  userId: Int!
  user: User!
  languageName: String!
  language: Language!
  createdAt: DateTime!
  updatedAt: DateTime!
  completedAt: DateTime
}

extend type Query {
  worksheets: [Worksheet!]!
  worksheet(id: Int!): Worksheet
}